language: node_js
sudo: false,
node_js:
  - "stable"
script:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep-history: true
  local-folder: build
  on:
    branch: master
